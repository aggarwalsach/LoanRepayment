spring:
  datasource:
    url: jdbc:mysql://localhost:3306/loan_table_schema?createDatabaseIfNotExist=true
    username: root
    password: Agar@nichas06
  application:
    name: LoanRepayment
  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
